<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/auth/login" access="permitAll" />
        <intercept-url pattern="/auth/logout" access="permitAll" />
        <intercept-url pattern="/auth/accessdenied" access="permitAll" />
        <!--<intercept-url pattern="/push/**" access="hasRole('ROLE_PUSH')" />-->
        <intercept-url pattern="/rest/**" access="hasRole('ROLE_REST')" />
        <intercept-url pattern="/admin" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/admin/" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/auth/**" access="permitAll" />
        <form-login login-page="/auth/login" default-target-url="/"
                            authentication-failure-url="/auth/accessdenied" />
        <logout logout-success-url="/auth/logout" />
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_PUSH, ROLE_REST, ROLE_USER, ROLE_ADMIN" />
                <user name="push" password="push" authorities="ROLE_USER, PUSH_ROLE" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>